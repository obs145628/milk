

fn foo(x: i32, y: f32, z: i64) : void {

}

fn bar(x: i32, y: f32, z: i64) : f64 {
  return x + y + z;
}

fn my_id(x: i64) : i64 {
  return x;
}

fn is_digit(x: char): bool {
  return x >= '0' && x <= '9';
}
